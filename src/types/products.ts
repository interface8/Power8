export type Product = {
  id: string;
  name: string;
  description?: string;
  imageUrl?: string;
  price: number;
  stockQuantity: number;
  categoryName?: string;
  power?: string;
  capacity?: string;
  warranty?: number;
};
